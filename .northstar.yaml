schema_version: "1.1"

project:
  name: northstar-vocab-uat
  root: "/home/timmypai/apps/northstar-vocab-uat"

runtime:
  db_path: .northstar/runtime/control-plane.sqlite3
  host_adapter: codex
  development_capacity: 1
  release_capacity: 1
  heartbeat_interval_seconds: 30
  lease_timeout_seconds: 600
  child_timeout_seconds: 7200
  auto_release: true
  session_scope: stage_root

workflow:
  package: builtin
  id: issue_to_pr_release
  version: "1.0"
  domain: software_development

github:
  repo: paulpai0412/northstar-vocab-uat
  intake:
    enabled: true
    label: northstar:ready
  sync:
    enabled: true
    retry_backoff_seconds:
      - 30
      - 120
      - 600
  project:
    enabled: false

git:
  base_branch: main
  worktrees_dir: .northstar/runtime/worktrees
  sync_worktree_dir: .northstar/runtime/sync-worktrees/main

credentials:
  github:
    token_env: GITHUB_TOKEN
    allow_gh_token_fallback: true
  host_sdk:
    codex:
      mode: sdk_default
    opencode:
      mode: sdk_default

policy:
  github_sync_blocks_lifecycle: false
  quarantine_requires_operator: true
